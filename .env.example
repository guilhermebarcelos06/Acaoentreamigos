# Variáveis usadas pelos scripts Node em scripts/ (migração de dados e import
# do banco do Rodrigo). NUNCA usar a service_role key no frontend.
# Copie para .env e preencha.

# Local (via `supabase start`):
# SUPABASE_URL=http://127.0.0.1:54321
# SUPABASE_SERVICE_ROLE_KEY=<SERVICE_ROLE_KEY impresso por `supabase start`>

# Produção (projeto "acaoentreamigos" — pegar a service_role key no dashboard,
# nunca commitar o valor real):
SUPABASE_URL=https://kicposaltebnfatqhitv.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
